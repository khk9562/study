---
title: "가운데 글자 가져오기"
tags: ["JavaScript", "Python3", "level.01", "코딩테스트", "프로그래머스"]
date: 2024-05-01
velog_id: 9d0a6389-5bb4-43a9-8b3a-e6c92a63101d
velog_url: https://velog.io/@steela/가운데-글자-가져오기
velog_updated: 2026-06-08T06:43:52.324Z
synced_at: 2026-06-28
---

> 🔗 원본: [velog.io/@steela/가운데-글자-가져오기](https://velog.io/@steela/가운데-글자-가져오기) · 📅 2024-05-01
# 문제 설명
단어 s의 가운데 글자를 반환하는 함수, solution을 만들어 보세요. 단어의 길이가 짝수라면 가운데 두글자를 반환하면 됩니다.

### 제한사항
s는 길이가 1 이상, 100이하인 스트링입니다.

<br />

# 문제 풀이 - Python3

### 1차 시도
파라미터 s의 길이를 측정하여 홀수 짝수를 나눠 리턴값을 구분하려고 했다.
```
def solution(s):
    if len(s) % 2 == 0:
        return s[len(s)/2 - 1 : len(s)];
    else :
        return s[len(s)/2]
```

오류 발생ㅠ

**TypeError: slice indices must be integers or None or have an __index__ method**

슬라이싱 값에서 정수만 사용가능한데, "/"를 사용하는 바람에 소수점이 허용되므로 "//"을 사용해야한다..!

### 2차 시도
그리고 len함수는 변수처리 해주고, 잘못된 인덱스 값을 수정해주었다.
```
def solution(s):
    length = len(s);
    if len(s) % 2 == 0:
        return s[length // 2 - 1 : length // 2 + 1];
    else :
        return s[length // 2]
```
성공!

<br/>

# 문제 풀이 - Javascript
Python의 slicing 기능 대신 문자열을 인덱스를 사용해서 추출하는 함수가 무엇인지 먼저 검색해보았다. 기존에 알고 있는 함수는 slice였고, substr과 substring의 존재만 알고 있었다.

> **substr("시작 위치", "길이") 또는 substr("시작 위치")**
substr() 함수는 시작 위치부터 해당 길이만큼 문자열을 자르는 아주 기본적인 함수이다. "길이" 부분을 생략하면 시작 위치부터 문자열 끝까지 자른다.

> **substring("시작 위치", "종료 위치") 또는 substring("시작 위치")**
substring() 함수는 시작 위치에서 종료 위치까지 문자열을 자른다. 주의할 점은 종료 위치의 -1까지 문자열을 자른다는 것이다.

> **slice("시작 위치", "종료 위치") 또는 slice("시작 위치")**
slice() 함수는 기본적인 사용법은 substring() 함수와 동일하며, 다른 점은 음수(-)를 자유롭게 사용할 수 있어서 뒤에서부터 문자열을 자를 때 유용하게 사용할 수 있다.

```
function solution(s) {
    l = s.length;
    if (l % 2 == 0) {
        return s.substr(l/2 - 1, 2)
    } else {
        return s.substr(l/2, 1)
    }
}
```
여기서는 substr을 사용해보았다.
단번에 성공!

### 다른 사람 풀이 참고
```
function solution(s) {
    return s.substr(Math.ceil(s.length / 2) - 1, s.length % 2 === 0 ? 2 : 1);
}
```

if 연산자를 삼항연산자로 변경해주고, true/false를 구분하는 조건을 Math.ceil(s.length / 2)로 해주었다. 나도 단번에 이렇게 생각해내고 싶다.... 더 열심히 하자!
