---
title: "로그인 및 보안"
tags: []
date: 2025-05-08
notion_id: 1ed922cf-26a8-80d4-83d9-c5cd6486b415
notion_last_edited: 2026-06-28T08:31:00.000Z
synced_at: 2026-06-28
---
> 📅 **학습일**: 2025-05-08

TODO
제안

- ~~API 접근 권한, 토큰 재발급 권한
get api res user_id로 권한 프론트에서 처리하는거 제발 그만ㅠㅠ
토큰 재발급 -> 로그인 전체 보안 관련 다시 고려 필요../~~
- Access Token은 사용자의 인증과 권한을 나타내는 단기 유효 토큰(문자열)으로, 클라이언트가 서버에 API 요청을 보낼 때 이 토큰을 헤더에 포함하여 요청을 보내고 액세스 토큰이 유효한지를 확인하여 사용자를 검증한다.

[https://suloth.tistory.com/211](https://suloth.tistory.com/211)


로컬 스토리지(Local Storage)
세션 스토리지(Session Storage)
쿠키(Cookies)
HttpOnly 플래그를 사용하여 JavaScript에서 쿠키에 접근할 수 없게 함으로써 XSS 공격에 대한 보안을 높이는 방식
Secure 플래그를 사용하면 HTTPS 연결에서만 쿠키가 전송되게 하여 중간자 공격(MITM)으로부터 쿠키를 보호할 수 있으며, SameSite 쿠키 속성을 설정하면 CSRF(교차 사이트 요청 위조) 공격을 방어


Access Token과 Refresh Token의 로그아웃 방식
사용자가 로그아웃 요청을 했을 때, Refresh Token 역시 로그아웃 조치를 취하여야 한다. 각 토큰들이 저장하는 곳에 따라 다음과 같은 방식으로 로그아웃을 진행한다.

1. 메모리, 토큰 Null 처리하기
2. HTTP-Only 쿠키, Set-Cookie 헤더를 통해 쿠키를 만료시키기
3. 서버, DB에서 토큰 제거하기
4. BlackList에 추가하기

지금 당장 적용 가능한 방법

1. 액세스토큰 만료기간 확인
2. 리프레시 토큰 요청 로직 추가
3. RTR(Refresh Token Rotation) 기법으로 RTK 탈취 피해 최소화 고려
액세스토큰 만료 후 리프레시 토큰 요청할 때 새로운 리프레시 토큰 함께 발급

추가 고려

1. Access Token (메모리) + Refresh Token(HttpOnly 쿠키) + Refresh Token 유효성(서버 관리)
2. Access Token (HttpOnly 쿠키) + Refresh Token(HttpOnly 쿠키)
3. Access Token (메모리 / HttpOnly 쿠키) + Refresh Token(서버)
